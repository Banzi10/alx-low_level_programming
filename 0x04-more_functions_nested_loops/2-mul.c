#include "main.h"
/**
  * mul - multiply two integers
  * @a: first integer to be mutliplied
  * @b: second integer
  * Return: always 0
  */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
